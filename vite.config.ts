import { defineConfig } from 'vite';

export default defineConfig({
    // plugins: [
    //     {
    //         name: 'optionHotUpdate',
    //         handleHotUpdate(ctx) {

    //         }
    //     }
    // ]
});